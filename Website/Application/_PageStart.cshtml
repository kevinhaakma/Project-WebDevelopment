@{
    Response.CacheControl = "no-cache";

    if (!WebSecurity.IsAuthenticated)
    {
        Response.Redirect("~/");
    }
    else if (WebSecurity.IsAuthenticated && Roles.IsUserInRole("administrator"))
    {
        Response.Redirect("~/Admin/registersecret");
    }
}