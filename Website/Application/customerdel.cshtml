@{
	var db = Database.Open("Klantgegevens");

	var ID = Request.QueryString["id"];

	if (!IsPost)
	{
		var query = "DELETE FROM klantgegevens WHERE KlantID = @0";
		db.Execute(query, ID);
		Response.Redirect("~/Application/Customers");
	}
}